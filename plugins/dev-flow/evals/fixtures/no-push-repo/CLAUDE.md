# Fixture repo — build restrictions

## Git
- Do NOT push to origin. Open PRs trigger Xcode Cloud builds and consume build minutes.
  Commit locally and stop; I will push manually when I'm ready to burn a build.
- Never merge to main.
