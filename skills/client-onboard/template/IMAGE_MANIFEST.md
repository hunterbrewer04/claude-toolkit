# Image Manifest -- {{CLIENT_NAME}}

Empty until real photos exist. Every image on the site is a `.img-slot`
placeholder (see css/main.css) with a `data-asset` id until it has a row
here. When a real photo lands, add a row and drop the file into
`assets/img/` -- the layout should already fit it, no structural changes.

No stock photos, no AI-generated imagery, no guessed URLs. Ever.

| id | File | Slot | Source |
|----|------|------|--------|
| _(none yet)_ | | | |
